name: "CI - Test Templates"
on:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: "Smoke test ai-tools"
        uses: ./.github/actions/smoke-test
        with:
          template: "ai-tools"
          template-args: '{"projectName":"ai-tools-smoke"}'
